import { ResumeOnboarding } from "@/components/creation/resume-onboarding";

// Resume creation entry point: shows the onboarding flow for starting a new resume.
export default function ResumeCreationMenuPage() {
  return <ResumeOnboarding />;
}
